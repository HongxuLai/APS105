#include <stdio.h>

int sortArray(int inArray[],int outArray[], int sizeOfInArray);

int main()
{
    return 0;
}

int sortArray(int inArray[],int outArray[], int sizeOfInArray)
{
    int countArray[600];

    for (int i = 0; i < sizeOfInArray; i ++)
    {
        
    }
}